name: Playwright QA

on:
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install deps
        run: |
          pip install playwright
          python -m playwright install

      - name: 24f2001824@ds.study.iitm.ac.in scraping step
        run: python scrape.py
